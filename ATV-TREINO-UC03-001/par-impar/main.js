let numero;

numero=prompt ("Digite um número");

if (numero % 2=== 0) {
    alert(" Número par!")
} else {
    alert(" Número ímpar!")
}

// = operador de atribuição
// == compara o valor e não o tipo (inteiro, texto, booleano, etc.)
// === compara valor e tipo
// !== operador de diferente
// ! operador de negação